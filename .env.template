# ============================================
# Travel Agent Application — Environment Variables
# ============================================
# Copy this file to .env and fill in real values.
#   cp .env.template .env
#
# NEVER commit .env to source control.
# ============================================

# Authentication: run 'az login' locally.
# In Azure, the app uses managed identity automatically.

# Azure AI Foundry (no API key — uses DefaultAzureCredential)
AZURE_AI_PROJECT_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o

# Application
APP_VERSION=0.1.0

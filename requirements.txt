
# Core ML / data
numpy
pandas
scikit-learn

# Visualization (optional but common)
matplotlib

# Notebooks
jupyter
ipykernel

# Utilities
tqdm

# Testing
pytest
pytest-cov
pytest-asyncio

# Lint/format (optional but recommended)
ruff

# Microsoft Agent Framework
# agent-framework-azure-ai is imported lazily inside agent creation functions
# to avoid transitive import conflicts between the Azure AI provider package
# and the underlying agent-framework-core stable releases.
agent-framework
agent-framework-azure-ai
agent-framework-orchestrations

# Web API
fastapi
uvicorn[standard]

# Configuration
python-dotenv
pydantic-settings

# HTTP client
httpx

# Azure Identity (for DefaultAzureCredential)
azure-identity
